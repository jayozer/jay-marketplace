schema_version: "1.1"
name: 12-draft-broad-feature
tags: [goal-orchestrator, draft-mode]
context:
  # Copies fixtures/sample-repo into the sandbox cwd; requires --scaffold on the run.
  scaffold_script: scaffold.sh
