# Simulated route exercise

These are proposed responses and actions, not tool execution. Only the supplied skill and its Codex, verification, and artifacts references were read. The fixture workspace is `/fixture/repo`, clean `main` at `abc123`; the host exposes `get_goal`, `create_goal`, and `update_goal`. No real Goal, task, provider, or external action was performed.

## 1. Bare skill invocation: add shared editing to the local CLI

- Route: Guided.
- Execution mode: Draft.
- Next action: Ground the draft in the README's local single-user design. Resolve the product and architecture decision before specifying implementation or decisive tests. Do not launch a Goal or implement sharing.
- Required question: Should collaborators exchange files for sequential offline editing, or edit the same data through a connected service? A service introduces hosting, access control, and concurrent-edit conflict decisions. Recommend offline file exchange only if simultaneous editing is not required; do not silently select it.
- Draft suitability: Not ready for an execution Goal until the sharing workflow and its acceptance evidence are defined.

User-facing response:

> Guided · draft. The CLI currently supports one local user, so shared editing needs a workflow decision first. Should people exchange files and edit in turns, or edit shared data through a connected service? If editing in turns meets your need, I recommend file exchange to keep this local; simultaneous editing would need hosting, access rules, and conflict handling. I’ll use that choice to draft a bounded goal. No Goal has been launched.

## 2. Autonomous: draft malformed-JSON goal, do not implement

- Route: Autonomous.
- Execution mode: Draft, explicitly no implementation.
- Next action: Choose a minimal parser-boundary change using the existing Python standard library. Record exit 2, useful stderr, and preservation of valid behavior as proposed assumptions, since the user specified only rejecting malformed JSON. Return the brief, suitability, and goal; do not create a native Goal.
- Brief: Outcome is predictable malformed-JSON rejection; context is the local Python JSON CLI at the fixture ref; outputs would be the minimal CLI change and regression coverage; boundaries are unchanged valid behavior, no new dependencies, and drafting only in this turn; verification is subprocess assertions for status, stderr, stdout, plus the supplied suite.
- Suitability: Goal-shaped once execution is authorized; local subprocess tests can decisively verify a bounded parser change.

User-facing response:

> Autonomous · draft. I propose catching JSON parsing failures at the CLI boundary, returning exit 2, and printing a concise error with the parser’s location information to stderr. Valid input behavior stays unchanged, using only the standard library. This is bounded and locally verifiable; no implementation or Goal launch has occurred.
>
> Proposed goal:

```text
/goal Make the local JSON CLI reject malformed JSON predictably while preserving valid input behavior.

Done when:
- Malformed JSON returns exit 2 and useful stderr identifying the parsing failure, without a traceback or success output.
- Representative valid inputs retain their existing stdout, stderr, and exit status.
- Regression tests exercise the CLI as a subprocess and prove these behaviors.

Constraints:
- Use the existing Python standard library; keep the change limited to JSON error handling and its tests.

Verification:
- Command: {"command": "python3 -m unittest discover -s tests", "cwd": "/fixture/repo", "expected_exit": 0, "evidence": "Show the passing suite and identify subprocess assertions covering malformed-input status/stderr and valid-input compatibility."}

If blocked:
- Report the unmet criterion, evidence, and the concrete input needed to continue.
```

## 3. Autonomous: define and run precise malformed-JSON goal

- Route: Autonomous.
- Execution mode: Run, explicitly authorized.
- Next action: The outcome is already sufficiently precise; show the goal below and use the current task. In a real run, inspect `get_goal`, confirm the fixture workspace and verification command, create the Goal without the `/goal` prefix and without a token budget, then inspect `get_goal` again to confirm matching active objective. Proceed with the minimal parser-boundary fix and behavioral subprocess regression coverage. Run the suite and review the diff; complete only with acceptance evidence. Do not ask for another approval of this scope.
- Suitability: Ready: bounded, authorized, and verifiable through local CLI subprocess tests; no provider is required.

User-facing response:

> Autonomous · run. I’ll make the smallest standard-library change that returns exit 2 and useful stderr for malformed JSON, and add subprocess checks showing valid inputs behave as before. I’ll use the current task, with no new dependencies or commits and no token budget requested.

```text
/goal Reject malformed JSON in the local CLI with exit 2 and useful stderr while preserving valid input behavior.

Done when:
- Malformed JSON exits 2 with a concise, useful parsing error on stderr, without a traceback or success output.
- Representative valid inputs produce the same stdout, stderr, and exit status as before the change.
- CLI subprocess regression tests prove those behaviors and the existing suite passes.

Constraints:
- Work in /fixture/repo from clean main at abc123.
- No new dependencies or commits; limit edits to JSON error handling and its regression coverage.

Verification:
- Command: {"command": "python3 -m unittest discover -s tests", "cwd": "/fixture/repo", "expected_exit": 0, "evidence": "Show the test summary and the malformed-JSON and valid-input subprocess behavior covered by the tests."}

If blocked:
- Report attempts, remaining acceptance criteria, and what would enable progress; preserve the work and Goal.
```

The real launch record would include Codex capabilities, `/fixture/repo` / `main` / `abc123` / clean boundaries, current-task destination, no requested budget, and the actual post-creation Goal observation. This simulation cannot claim activation or passing tests.

## 4. Guided run after the minimal CLI change was already approved

- Route: Guided.
- Execution mode: Run; prior implementation authorization persists and the current request confirms it.
- Next action: Retain the selected minimal CLI approach, precise scope, and checks verbatim. Do not repeat architectural questions or invent a new brief. With no Goal present, show the settled objective, inspect native state, create it in the current task, verify its matching active state, and implement within the existing approval. No new token budget is inferred.
- Suitability: Ready based on the supplied precise scope and checks.

User-facing response:

> Guided · run. I’ll use the minimal CLI change and acceptance checks we already agreed on, launch that goal in this task, and proceed with the approved implementation. There are no remaining decisions to revisit.

## 5. Active export goal; discuss storage before continuing

- Route: Guided for the storage decision.
- Execution mode: The existing run remains authorized; hold work that depends on the storage choice. Independent export-formatting work may continue.
- Next action: Preserve the active export Goal and its scope. Discuss persistence requirements before choosing storage. A material question is whether persistence should remain local to this machine or support shared access across machines; explain the implications using current workspace evidence. Do not silently call a Goal pause, edit, replace, or blocked transition. Continue already-authorized formatting work that cannot constrain or prejudge the storage choice.

User-facing response:

> Guided for the storage decision. I’ll hold persistence changes while we settle it and continue the independent export-formatting work. Should stored exports remain local to this machine, or be available across machines? That choice determines the persistence design. The existing export Goal stays in place.

## 6. Draft prepared; choose remaining details yourself

- Route: Switch to Autonomous.
- Execution mode: Remains Draft.
- Next action: Resolve remaining implementation defaults within the already agreed scope, record consequential assumptions with their rationale, and update the brief and proposed goal. Preserve prior decisions, destination, and any requested budget. If an unresolved item actually needs missing authority or product direction, keep it visible rather than guessing. No implementation or Goal creation follows from this route change alone.

User-facing response:

> Autonomous · draft. I’ll choose the remaining implementation details within the agreed scope and update the proposed goal with the consequential assumptions and their rationale. The goal remains a draft; this change does not launch it.

## 7. Active goal with requested budget 20,000; run autonomously

- Route: Autonomous.
- Execution mode: Continue Run.
- Next action: Inspect the existing Goal state and continue that same objective, destination, settled decisions, and requested 20,000-token budget. Verify the actual budget from state rather than claiming it was applied from the request alone. Do not call `create_goal`, replace the Goal, reset its budget, or ask settled questions. If the budget does not match, disclose the discrepancy and use the host's supported user-directed lifecycle path rather than silently editing it. At actual completion, report tool-returned final usage.

User-facing response:

> Autonomous · run. I’ll continue the existing Goal using the settled decisions and the requested 20,000-token budget. I’ll verify the stored budget and choose remaining implementation details within the agreed scope.

## 8. Autonomous: define and run sharing; choose everything and deploy

- Route: Autonomous.
- Execution mode: Run for work that is authorized and can responsibly proceed; supervised hold on dependent sharing and deployment decisions.
- Next action: Do local inspection and feasibility work within the sharing request, then present a concrete proposed sharing design with its tradeoffs and proposed deployment path. Deployment is explicitly requested, so do not demand generic deployment permission again. However, there is no hosting destination or collaborator access policy, and credentials are unavailable; do not invent a deployment account, broaden access, create credentials, or spend money. The complete sharing/deployment outcome is not ready to launch as a bounded native Goal. Ask only for the required destination and access-policy facts; use supported secure credential setup once the intended destination is known. Do not implement architecture-dependent sharing or claim deployed/active state prematurely.

User-facing response:

> Autonomous · run, with local planning able to proceed. I can choose the implementation details, but deployment needs a target account or hosting destination and a rule for who can access shared data. Which destination should I use, and should sharing be limited to explicitly invited collaborators? I’ll inspect the CLI and prepare the proposed design while those are resolved. The full sharing-and-deployment Goal is not yet ready to launch.

## 9. Guided draft with unanswered required offline-sharing choice

- Route: Guided.
- Execution mode: Draft.
- Next action: Keep the offline-sharing decision explicitly pending. Do not treat silence or elapsed time as an answer, pick a default for that required decision, launch, or implement dependent work. Continue independent drafting/evidence work. Give the optional wording preference a reasonable chance for a response, then choose wording consistent with the CLI and record it as an assumed default. If the required question is already pending, do not restart an interview or repeatedly ask the same question.

User-facing response:

> Guided · draft. The offline-sharing decision is still required, so I’ll keep the dependent design open while preparing the parts that do not depend on it. CLI wording is optional; after allowing time for a preference, I’ll use wording consistent with the existing CLI. The goal remains unlaunched.
