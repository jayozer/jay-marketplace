---
max_turns: 15
timeout_seconds: 360
allowed_tools: [Read, Glob, Grep, Skill]
runs: 3
---
Below are the requirements our team wrote for a command-line wrapper around the calc package in this repo. Use goal-orchestrator to draft the goal for building it. Do not start it.

Requirements:
R01 Argument Parsing: The calc CLI must handle argument parsing in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R02 Help Text: The calc CLI must handle help text in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R03 Exit Codes: The calc CLI must handle exit codes in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R04 Error Messages: The calc CLI must handle error messages in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R05 Logging: The calc CLI must handle logging in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R06 Configuration File: The calc CLI must handle configuration file in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R07 Environment Variables: The calc CLI must handle environment variables in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R08 Output Formatting: The calc CLI must handle output formatting in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R09 Json Output Mode: The calc CLI must handle JSON output mode in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R10 Quiet Mode: The calc CLI must handle quiet mode in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R11 Verbose Mode: The calc CLI must handle verbose mode in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R12 Input Validation: The calc CLI must handle input validation in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R13 Negative Numbers: The calc CLI must handle negative numbers in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R14 Floating Point: The calc CLI must handle floating point in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R15 Integer Overflow: The calc CLI must handle integer overflow in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R16 Division By Zero: The calc CLI must handle division by zero in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R17 Locale Handling: The calc CLI must handle locale handling in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R18 Shell Completion: The calc CLI must handle shell completion in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R19 Man Page: The calc CLI must handle man page in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R20 Packaging: The calc CLI must handle packaging in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R21 Versioning: The calc CLI must handle versioning in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R22 Release Notes: The calc CLI must handle release notes in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R23 Backward Compatibility: The calc CLI must handle backward compatibility in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R24 Deprecation Policy: The calc CLI must handle deprecation policy in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R25 Unit Tests: The calc CLI must handle unit tests in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R26 Integration Tests: The calc CLI must handle integration tests in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R27 Smoke Tests: The calc CLI must handle smoke tests in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R28 Ci Workflow: The calc CLI must handle CI workflow in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R29 Code Style: The calc CLI must handle code style in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
R30 Type Checking: The calc CLI must handle type checking in a way that is consistent with common Unix conventions. Behaviour must be documented in the README, covered by at least one automated test, and must not change the existing add function's public signature. Any deviation from this requirement needs a written rationale in the pull request description.
